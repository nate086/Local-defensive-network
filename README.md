# Local-defensive-network
Local Defense Network (LDN) is a sub-millisecond, air-gapped execution gate for AI agent tools. Operating at the process boundary, LDN intercepts parameter calls in &lt;0.01ms to block prompt injections, path traversals, and unauthorized commands using deterministic rules rather than slow, probabilistic LLM checks.
